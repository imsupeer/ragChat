# llama.cpp Process State

When you start `llama-server` via the project skeleton scripts, a PID file may be written here:

- `llama-server.pid`

Do not commit PID files. Stop the server with `./stop.sh` or `.\stop.ps1`.
